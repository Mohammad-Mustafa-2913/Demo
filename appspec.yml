version: 0.0
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
hook:
  BeforeInstall:
    - location: scripts/install_dependencies
      timeout: 300
      rumas: root
    - location: scripts/start_server
      timeout: 300
      rumas: root
